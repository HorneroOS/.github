{
  "name": "@horneroos/pr-governance",
  "version": "0.1.0",
  "description": "Shared deterministic Danger (TypeScript) PR-governance engine for HorneroOS.",
  "private": true,
  "type": "commonjs",
  "engines": {
    "node": "22.22.0"
  },
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "clean": "rm -rf dist",
    "test": "npm run clean && npm run build && cp -r tests/fixtures dist/tests/fixtures && node --test dist/tests/*.test.js",
    "danger": "danger ci"
  },
  "dependencies": {
    "danger": "12.3.3",
    "js-yaml": "4.1.0"
  },
  "devDependencies": {
    "@types/js-yaml": "4.0.9",
    "@types/node": "22.13.5",
    "typescript": "5.7.3"
  }
}
